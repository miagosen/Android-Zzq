<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zzq.draw"
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="16" />

    <application
        android:icon="@drawable/icon"
       android:name="com.zzq.draw.KeTuKeLeApplication"
       android:theme="@style/ActionBarBaseTheme"
       >
        <activity
            android:name="com.zzq.draw.activity.MainActivity"
            android:label="@string/app_name"
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 去登录页面Activity -->
        <activity
            android:name="com.zzq.draw.activity.login.LoginActivity"
            android:label="@string/login">

        </activity>
    </application>

</manifest>